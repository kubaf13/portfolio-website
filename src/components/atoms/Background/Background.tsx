import React from 'react';

import { StyledP } from './Background.styled';
import type { Background as BackgroundType } from './Background.types';

export const Background: BackgroundType = () => (
  <>
    <StyledP>COMPONENT: Background</StyledP>
    <div />
  </>
);
